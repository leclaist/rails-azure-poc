#!/usr/bin/env bash
set -e

echo "== Building development image (downloads Oracle Instant Client — takes a few minutes on first run) =="
docker-compose build web

echo ""
echo "== Setup complete =="
echo "  bin/dev   — start the app at http://localhost:3000"
echo "  bin/test  — run the test suite"
